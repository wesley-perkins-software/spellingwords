---
interface Props {
  currentPath?: string;
}
const { currentPath = '' } = Astro.props;

const links = [
  { href: '/design-explore/exploration-c/', label: 'Home' },
  { href: '/design-explore/exploration-c/grade-hub.html', label: 'Grades' },
  { href: '/design-explore/exploration-c/skill.html', label: 'Skills' },
  { href: '/design-explore/exploration-c/practice.html', label: 'Practice' },
];
---

<header class="site-header">
  <div class="wrap header-inner">
    <a class="wordmark" href="/design-explore/exploration-c/">
      <span class="wordmark-mark" aria-hidden="true">Sw</span>
      SpellingWords
    </a>
    <nav class="head-nav" aria-label="Primary">
      <ul>
        {
          links.map((link) => (
            <li>
              <a href={link.href} aria-current={currentPath === link.href ? 'page' : undefined}>
                {link.label}
              </a>
            </li>
          ))
        }
      </ul>
    </nav>
    <a class="header-cta" href="/design-explore/exploration-c/practice.html">Start practicing</a>
  </div>
</header>

<style>
  .site-header {
    border-bottom: 1px solid var(--border);
    background: var(--canvas-raised);
    position: sticky;
    top: 0;
    z-index: 40;
  }

  .header-inner {
    display: flex;
    align-items: center;
    gap: 1.75rem;
    padding-block: 1rem;
  }

  .wordmark {
    display: inline-flex;
    align-items: center;
    gap: 0.65rem;
    font-family: var(--font-display);
    font-weight: 700;
    font-size: 1.1rem;
    color: var(--ink);
    text-decoration: none;
    flex-shrink: 0;
  }

  .wordmark-mark {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 2.15rem;
    height: 2.15rem;
    border-radius: var(--radius-sm);
    background: var(--ink);
    color: var(--canvas);
    font-size: 0.82rem;
    font-weight: 700;
  }

  .head-nav {
    flex: 1;
  }

  .head-nav ul {
    display: flex;
    gap: 2rem;
    list-style: none;
    margin: 0;
    padding: 0;
  }

  .head-nav a {
    display: inline-block;
    text-decoration: none;
    font-weight: 600;
    font-size: 0.92rem;
    color: var(--ink-soft);
    padding: 0.45rem 0.05rem;
    border-bottom: 2px solid transparent;
    transition: color 0.12s ease, border-color 0.12s ease;
  }

  .head-nav a:hover {
    color: var(--ink);
    border-bottom-color: var(--border-strong);
  }

  .head-nav a[aria-current='page'] {
    color: var(--ink);
    border-bottom-color: var(--practice-vivid);
  }

  .header-cta {
    flex-shrink: 0;
    display: inline-flex;
    align-items: center;
    gap: 0.4rem;
    background: var(--practice-strong);
    color: #fff;
    font-weight: 700;
    font-size: 0.88rem;
    text-decoration: none;
    padding: 0.62rem 1.2rem;
    border-radius: var(--radius-pill);
    transition: background 0.15s ease, transform 0.1s ease;
  }

  .header-cta:hover {
    background: var(--practice-ink);
  }

  @media (max-width: 720px) {
    .head-nav {
      display: none;
    }
    .header-cta {
      margin-left: auto;
      padding: 0.55rem 0.9rem;
      font-size: 0.82rem;
    }
  }
</style>
