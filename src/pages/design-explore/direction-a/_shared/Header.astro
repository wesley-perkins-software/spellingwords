---
interface Props {
  currentPath?: string;
}
const { currentPath = '' } = Astro.props;

const links = [
  { href: '/design-explore/direction-a/', label: 'Home' },
  { href: '/design-explore/direction-a/grade-hub', label: 'Grades' },
  { href: '/design-explore/direction-a/skill', label: 'Skills' },
  { href: '/design-explore/direction-a/practice', label: 'Practice' },
];
---

<header class="site-header">
  <div class="wrap header-inner">
    <a class="wordmark" href="/design-explore/direction-a/">
      <span class="wordmark-mark" aria-hidden="true">Sw</span>
      SpellingWords
    </a>
    <nav class="head-nav" aria-label="Primary">
      <ul>
        {
          links.map((link) => (
            <li>
              <a href={link.href} aria-current={currentPath === link.href ? 'page' : undefined}>
                {link.label}
              </a>
            </li>
          ))
        }
      </ul>
    </nav>
    <a class="header-cta" href="/design-explore/direction-a/practice">Start practicing</a>
  </div>
</header>

<style>
  .site-header {
    border-bottom: 3px solid var(--brand);
    background: var(--canvas-raised);
    position: sticky;
    top: 0;
    z-index: 40;
  }

  .header-inner {
    display: flex;
    align-items: center;
    gap: 1.5rem;
    padding-block: 0.9rem;
  }

  .wordmark {
    display: inline-flex;
    align-items: center;
    gap: 0.6rem;
    font-family: var(--font-display);
    font-weight: 700;
    font-size: 1.2rem;
    color: var(--ink);
    text-decoration: none;
    flex-shrink: 0;
  }

  .wordmark-mark {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 2.1rem;
    height: 2.1rem;
    border-radius: var(--radius-sm);
    background: var(--brand);
    color: #fff;
    font-family: var(--font-display);
    font-size: 0.9rem;
    font-weight: 700;
  }

  .head-nav {
    flex: 1;
  }

  .head-nav ul {
    display: flex;
    gap: 1.75rem;
    list-style: none;
    margin: 0;
    padding: 0;
  }

  .head-nav a {
    text-decoration: none;
    font-weight: 700;
    font-size: 0.94rem;
    color: var(--ink-soft);
    padding: 0.5rem 0.15rem;
    border-bottom: 3px solid transparent;
    display: inline-flex;
    align-items: center;
    min-height: 44px;
  }

  .head-nav a:hover {
    color: var(--brand);
  }

  .head-nav a[aria-current='page'] {
    color: var(--brand);
    border-bottom-color: var(--coral);
  }

  .header-cta {
    flex-shrink: 0;
    background: var(--coral);
    color: #ffffff;
    font-weight: 700;
    font-size: 0.9rem;
    text-decoration: none;
    padding: 0.7rem 1.25rem;
    border-radius: var(--radius-sm);
    transition: background 0.15s ease;
    min-height: 44px;
    display: inline-flex;
    align-items: center;
  }

  .header-cta:hover {
    background: var(--coral-strong);
  }

  @media (max-width: 720px) {
    .head-nav {
      display: none;
    }
    .header-cta {
      margin-left: auto;
      padding: 0.6rem 1rem;
      font-size: 0.85rem;
    }
  }
</style>
