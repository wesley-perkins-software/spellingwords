---
// Stress test 2: same content family as Short A (short-vowels + cvc, so
// still word-family mode), but with a populated FAQ and a 4-item related
// list — both absent from Short A — and no linked Skill (skillIds: []),
// to prove the "Go deeper" section correctly disappears when there isn't one.
import GradeUnitWorldPage from '@/components/GradeUnitWorldPage.astro';
---

<GradeUnitWorldPage listId="kindergarten-short-i-words" />
