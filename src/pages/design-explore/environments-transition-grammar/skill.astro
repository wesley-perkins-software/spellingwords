---
// Design exploration — Shifting Environments, Variation B2
// Skill / reference page: opens with a threshold bar carrying the
// "Discovery → Reading" handoff (since a visitor most often arrives here
// from Home), settles into a flat reading environment, then crosses a
// second threshold bar into the practicing register before the CTA.
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="robots" content="noindex,nofollow" />
    <title>SH Digraph Words (Design exploration)</title>
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Fraunces:ital,opsz,wght@0,9..144,400;0,9..144,560;0,9..144,650;1,9..144,500&family=Inter:wght@400;500;600;700&family=Space+Grotesk:wght@500;600&display=swap"
      rel="stylesheet"
    />
  </head>
  <body>
    <div class="proto-badge" aria-hidden="true">Design exploration — Shifting Environments / B2: Transition grammar</div>

    <div class="page">
      <div class="threshold" style="--from: #c8632f; --to: #9a5a1f;" role="presentation">
        <span class="threshold-label threshold-label-left">Discovery</span>
        <span class="threshold-label threshold-label-right">Reading</span>
      </div>

      <div class="env-reading">
        <header class="topline">
          <a class="wordmark" href="/design-explore/environments-transition-grammar/">
            <span class="wordmark-mark">sw</span>spellingwords
          </a>
          <nav class="topnav" aria-label="Prototype navigation">
            <a href="/design-explore/environments-transition-grammar/">Home</a>
          </nav>
        </header>

        <main class="inner">
          <p class="crumb">
            <a href="/design-explore/environments-transition-grammar/">Home</a> /
            <a href="/design-explore/environments-transition-grammar/">Skills</a> / SH Digraph Words
          </p>

          <header class="hero">
            <h1>SH Digraph Words</h1>
            <p class="hero-desc">
              Two letters, one sound. The <strong>sh</strong> digraph is one of the first consonant
              teams kids learn to hear and write as a single unit, and it shows up everywhere from
              <em>ship</em> to <em>fish</em>.
            </p>
          </header>

          <section class="answer-block" aria-labelledby="what-h">
            <h2 id="what-h">What are SH digraph words?</h2>
            <p>
              SH digraph words contain the letter pair <strong>sh</strong>, which together make one
              sound &mdash; the quiet &ldquo;hush&rdquo; sound, as in <em>shop</em> or <em>wish</em>.
              Neither letter keeps its own sound; <strong>s</strong> and <strong>h</strong> combine
              into something new, which is exactly what makes a digraph a digraph rather than a
              blend.
            </p>
          </section>

          <section aria-labelledby="list-h" class="word-list-section">
            <h2 id="list-h">Word list</h2>
            <ul class="word-list">
              <li><span class="word">ship</span></li>
              <li><span class="word">shop</span></li>
              <li><span class="word">shut</span></li>
              <li><span class="word">wish</span></li>
              <li><span class="word">brush</span></li>
              <li><span class="word">dish</span></li>
            </ul>
          </section>

          <section class="prose" aria-labelledby="understanding-h">
            <h2 id="understanding-h">Understanding the pattern</h2>
            <p>
              The <strong>sh</strong> digraph can appear at the start of a word, as in <em>ship</em>
              and <em>shop</em>, or at the end, as in <em>wish</em> and <em>dish</em>. Either way, it
              behaves as a single spelling unit &mdash; kids should be encouraged to write it as one
              chunk, not sound out an /s/ and a /h/ separately and try to glue them together.
            </p>
            <p>
              This matters for spelling in particular because the <strong>sh</strong> sound has only
              one common spelling in early reading, unlike trickier sounds such as long <em>a</em>,
              which can be spelled several different ways. That makes SH words a reliable, confidence
              -building unit: once a child hears the sound, there&rsquo;s really only one letter pair
              to reach for.
            </p>
            <p>
              Where it gets more interesting is in longer words and word endings &mdash; words like
              <em>brush</em> and <em>crash</em> pair the digraph with a consonant blend, which is
              worth calling out explicitly once the short, simple SH words feel comfortable.
            </p>
          </section>

          <section aria-labelledby="compare-h" class="compare-section">
            <h2 id="compare-h">SH vs. CH</h2>
            <p class="compare-intro">
              SH and CH are easy to mix up early on, since both are two-letter consonant teams that
              make a single new sound. Saying the word slowly usually settles it.
            </p>
            <div class="table-wrap">
              <table>
                <thead>
                  <tr>
                    <th scope="col">Word</th>
                    <th scope="col">Digraph</th>
                    <th scope="col">Sound</th>
                    <th scope="col">Example sentence</th>
                  </tr>
                </thead>
                <tbody>
                  <tr>
                    <td>ship</td>
                    <td>sh</td>
                    <td>&ldquo;hush&rdquo; sound</td>
                    <td>The ship left the harbor at dawn.</td>
                  </tr>
                  <tr>
                    <td>chip</td>
                    <td>ch</td>
                    <td>&ldquo;ch&rdquo; sound</td>
                    <td>One chip fell off the plate.</td>
                  </tr>
                  <tr>
                    <td>wish</td>
                    <td>sh</td>
                    <td>&ldquo;hush&rdquo; sound</td>
                    <td>She made a wish before blowing out the candle.</td>
                  </tr>
                  <tr>
                    <td>witch</td>
                    <td>ch</td>
                    <td>&ldquo;ch&rdquo; sound</td>
                    <td>The witch wore a tall hat.</td>
                  </tr>
                </tbody>
              </table>
            </div>
          </section>

          <section class="faq" aria-labelledby="faq-h">
            <h2 id="faq-h">Frequently asked questions</h2>
            <div class="faq-item">
              <h3>Does every SH sound get spelled with the letters S and H?</h3>
              <p>
                In early reading, yes &mdash; almost always. Later on, kids will meet other spellings
                for the same sound (like the &ldquo;ti&rdquo; in <em>nation</em> or the &ldquo;ci&rdquo;
                in <em>special</em>), but those come well after the SH digraph is solid, so it&rsquo;s
                safe to treat <strong>sh</strong> as the default spelling for now.
              </p>
            </div>
            <div class="faq-item">
              <h3>Is SH a blend or a digraph?</h3>
              <p>
                A digraph. In a blend, like <em>bl</em> in <em>black</em>, you can still hear both
                letters. In a digraph, like <em>sh</em>, the two letters merge into one new sound
                with no trace of the individual <em>s</em> or <em>h</em>.
              </p>
            </div>
          </section>
        </main>
      </div>

      <div class="threshold" style="--from: #9a5a1f; --to: #2c4f7c;" role="presentation">
        <span class="threshold-label threshold-label-left">Reading</span>
        <span class="threshold-label threshold-label-right">Practicing</span>
      </div>

      <div class="env-practice">
        <div class="inner practice-inner">
          <section class="cta-block">
            <div class="cta-inner">
              <div>
                <p class="cta-eyebrow">Ready when you are</p>
                <h2>Practice these words</h2>
                <p>Six words, read aloud one at a time, typed by hand.</p>
              </div>
              <a class="cta-btn" href="/design-explore/environments-transition-grammar/skill">Start Practicing &rarr;</a>
            </div>
          </section>

          <section class="related" aria-labelledby="related-h">
            <h2 id="related-h">Related</h2>
            <ul>
              <li><a href="/design-explore/environments-transition-grammar/skill">CH Digraph Words</a></li>
              <li><a href="/design-explore/environments-transition-grammar/skill">TH Digraph Words</a></li>
              <li><a href="/design-explore/environments-transition-grammar/">1st Grade Spelling Words</a></li>
            </ul>
          </section>
        </div>
      </div>
    </div>

    <style>
      :global(html, body) {
        margin: 0;
        padding: 0;
      }
      * {
        box-sizing: border-box;
      }

      .proto-badge {
        position: fixed;
        bottom: 10px;
        right: 10px;
        z-index: 50;
        background: rgba(24, 24, 24, 0.82);
        color: #fdf6ec;
        font: 500 11px/1.4 'Inter', sans-serif;
        padding: 6px 10px;
        border-radius: 6px;
        pointer-events: none;
      }

      .page {
        font-family: 'Inter', system-ui, sans-serif;
        color: #201c14;
      }
      a {
        color: inherit;
      }
      *:focus-visible {
        outline: 3px solid #1c4f8c;
        outline-offset: 3px;
        border-radius: 4px;
      }

      .inner {
        max-width: 760px;
        margin: 0 auto;
        padding-inline: clamp(20px, 5vw, 64px);
      }

      .threshold {
        position: relative;
        height: 34px;
        background: linear-gradient(90deg, var(--from) 0%, var(--from) 50%, var(--to) 50%, var(--to) 100%);
      }
      .threshold-label {
        position: absolute;
        top: 50%;
        transform: translateY(-50%);
        font-family: 'Space Grotesk', sans-serif;
        font-size: 11px;
        font-weight: 600;
        letter-spacing: 0.08em;
        text-transform: uppercase;
        color: rgba(255, 255, 255, 0.88);
      }
      .threshold-label-left {
        right: calc(50% + 14px);
      }
      .threshold-label-right {
        left: calc(50% + 14px);
      }

      .env-reading {
        background: #f1e2bc;
        color: #362912;
        padding-bottom: clamp(2rem, 5vw, 3rem);
      }
      .env-practice {
        background: #dce6f2;
        color: #1c2e42;
        padding-block: clamp(2.5rem, 5vw, 3.5rem);
      }

      .topline {
        display: flex;
        align-items: center;
        justify-content: space-between;
        padding: 24px clamp(20px, 5vw, 64px) 8px;
        max-width: 760px;
        margin: 0 auto;
        flex-wrap: wrap;
        gap: 12px;
      }
      .wordmark {
        display: inline-flex;
        align-items: center;
        gap: 8px;
        font-family: 'Fraunces', serif;
        font-weight: 650;
        font-size: 20px;
        text-decoration: none;
        color: inherit;
      }
      .wordmark-mark {
        display: grid;
        place-items: center;
        width: 30px;
        height: 30px;
        border-radius: 9px;
        background: #9a5a1f;
        color: #fff;
        font-family: 'Space Grotesk', sans-serif;
        font-size: 13px;
        font-weight: 600;
      }
      .topnav {
        display: flex;
        gap: 22px;
        font-size: 14px;
        font-weight: 500;
      }
      .topnav a {
        text-decoration: none;
        color: inherit;
      }
      .topnav a:hover {
        text-decoration: underline;
      }

      .crumb {
        font-size: 13px;
        color: #93805a;
        margin: 0 0 28px;
        padding-top: 20px;
      }
      .crumb a {
        text-decoration: none;
        color: #93805a;
      }
      .crumb a:hover {
        text-decoration: underline;
      }

      .hero h1 {
        font-family: 'Fraunces', serif;
        font-weight: 650;
        font-size: clamp(32px, 5vw, 48px);
        margin: 0 0 16px;
        letter-spacing: -0.01em;
      }
      .hero-desc {
        font-size: 17px;
        line-height: 1.65;
        color: #6b5a38;
        max-width: 62ch;
        margin: 0 0 40px;
      }

      section {
        margin-bottom: 44px;
      }
      h2 {
        font-family: 'Fraunces', serif;
        font-weight: 560;
        font-size: 21px;
        margin: 0 0 14px;
      }

      .answer-block {
        border-left: 3px solid #b97a2f;
        padding-left: 20px;
      }
      .answer-block p {
        margin: 0;
        font-size: 16px;
        line-height: 1.7;
        color: #6b5a38;
        max-width: 60ch;
      }

      .word-list {
        list-style: none;
        margin: 0;
        padding: 0;
        display: flex;
        flex-wrap: wrap;
        gap: 0 40px;
        border-top: 1px solid rgba(54, 41, 18, 0.18);
        border-bottom: 1px solid rgba(54, 41, 18, 0.18);
        padding: 6px 0;
      }
      .word-list li {
        padding: 10px 0;
      }
      .word {
        font-family: 'Space Grotesk', sans-serif;
        font-weight: 600;
        font-size: 19px;
        letter-spacing: 0.01em;
      }

      .prose p {
        font-size: 16px;
        line-height: 1.75;
        color: #6b5a38;
        max-width: 64ch;
        margin: 0 0 18px;
      }
      .prose p:last-child {
        margin-bottom: 0;
      }

      .compare-intro {
        margin: 0 0 18px;
        font-size: 15px;
        color: #6b5a38;
        max-width: 62ch;
      }
      .table-wrap {
        overflow-x: auto;
      }
      table {
        width: 100%;
        border-collapse: collapse;
        font-size: 14.5px;
      }
      thead th {
        text-align: left;
        font-family: 'Space Grotesk', sans-serif;
        font-weight: 600;
        font-size: 12.5px;
        letter-spacing: 0.05em;
        text-transform: uppercase;
        color: #93805a;
        padding: 0 14px 10px 0;
        border-bottom: 2px solid rgba(54, 41, 18, 0.2);
      }
      tbody td {
        padding: 12px 14px 12px 0;
        border-bottom: 1px solid rgba(54, 41, 18, 0.1);
        color: #362912;
      }
      tbody tr:nth-child(odd) {
        background: rgba(154, 90, 31, 0.06);
      }

      .faq-item {
        border-top: 1px solid rgba(54, 41, 18, 0.14);
        padding-top: 18px;
        margin-bottom: 18px;
      }
      .faq-item h3 {
        font-family: 'Fraunces', serif;
        font-weight: 560;
        font-size: 17px;
        margin: 0 0 8px;
      }
      .faq-item p {
        margin: 0;
        font-size: 15px;
        line-height: 1.7;
        color: #6b5a38;
        max-width: 62ch;
      }

      .practice-inner {
        max-width: 760px;
      }
      .cta-block {
        background: #fff;
        border-radius: 16px;
        padding: 2px;
        border: 1px solid rgba(28, 47, 66, 0.12);
      }
      .cta-inner {
        border-radius: 15px;
        padding: 26px 28px;
        display: flex;
        align-items: center;
        justify-content: space-between;
        gap: 20px;
        flex-wrap: wrap;
      }
      .cta-eyebrow {
        margin: 0 0 4px;
        font-size: 12px;
        font-weight: 600;
        letter-spacing: 0.08em;
        text-transform: uppercase;
        color: #4a6a94;
      }
      .cta-inner h2 {
        color: #1c2e42;
        margin: 0 0 4px;
      }
      .cta-inner > div > p {
        margin: 0;
        color: #3f577a;
        font-size: 14px;
      }
      .cta-btn {
        text-decoration: none;
        font-weight: 600;
        font-size: 15px;
        color: #fff;
        background: #2c4f7c;
        padding: 12px 22px;
        border-radius: 999px;
        white-space: nowrap;
      }
      .cta-btn:hover {
        background: #1c3a60;
      }

      .related {
        margin-top: 36px;
        margin-bottom: 0;
      }
      .related ul {
        list-style: none;
        margin: 0;
        padding: 0;
        display: flex;
        flex-wrap: wrap;
        gap: 8px 22px;
      }
      .related a {
        text-decoration: none;
        font-size: 14.5px;
        font-weight: 500;
        color: #2c4f7c;
        border-bottom: 1.5px solid rgba(44, 79, 124, 0.35);
        padding-bottom: 2px;
      }
      .related a:hover {
        border-color: #2c4f7c;
      }

      @media (max-width: 560px) {
        .cta-inner {
          flex-direction: column;
          align-items: flex-start;
        }
        .threshold-label {
          font-size: 9.5px;
        }
        .threshold-label-left {
          right: calc(50% + 8px);
        }
        .threshold-label-right {
          left: calc(50% + 8px);
        }
      }
    </style>
  </body>
</html>
