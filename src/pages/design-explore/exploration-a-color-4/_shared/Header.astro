---
interface Props {
  currentPath?: string;
}
const { currentPath = '' } = Astro.props;

const links = [
  { href: '/design-explore/exploration-a-color-4/', label: 'Home' },
  { href: '/design-explore/exploration-a-color-4/grade-hub.html', label: 'Grades' },
  { href: '/design-explore/exploration-a-color-4/skill.html', label: 'Skills' },
  { href: '/design-explore/exploration-a-color-4/practice.html', label: 'Practice' },
];
---

<header class="site-header">
  <div class="wrap header-inner">
    <a class="wordmark" href="/design-explore/exploration-a-color-4/">
      <span class="wordmark-mark" aria-hidden="true">Sw</span>
      SpellingWords
    </a>
    <nav class="head-nav" aria-label="Primary">
      <ul>
        {
          links.map((link) => (
            <li>
              <a href={link.href} aria-current={currentPath === link.href ? 'page' : undefined}>
                {link.label}
              </a>
            </li>
          ))
        }
      </ul>
    </nav>
    <a class="header-cta" href="/design-explore/exploration-a-color-4/practice.html">Start practicing</a>
  </div>
</header>

<style>
  .site-header {
    border-bottom: 1px solid var(--border);
    background: var(--canvas-raised);
    position: sticky;
    top: 0;
    z-index: 40;
  }

  .header-inner {
    display: flex;
    align-items: center;
    gap: 1.5rem;
    padding-block: 0.9rem;
  }

  .wordmark {
    display: inline-flex;
    align-items: center;
    gap: 0.6rem;
    font-family: var(--font-display);
    font-weight: 700;
    font-size: 1.15rem;
    color: var(--ink);
    text-decoration: none;
    flex-shrink: 0;
  }

  .wordmark-mark {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 2rem;
    height: 2rem;
    border-radius: var(--radius-sm);
    background: var(--stage);
    color: var(--ink-on-stage);
    font-size: 0.85rem;
    font-weight: 700;
  }

  .head-nav {
    flex: 1;
  }

  .head-nav ul {
    display: flex;
    gap: 1.75rem;
    list-style: none;
    margin: 0;
    padding: 0;
  }

  .head-nav a {
    text-decoration: none;
    font-weight: 600;
    font-size: 0.92rem;
    color: var(--ink-soft);
    padding: 0.4rem 0.1rem;
    border-bottom: 2px solid transparent;
  }

  .head-nav a:hover {
    color: var(--ink);
  }

  .head-nav a[aria-current='page'] {
    color: var(--brand);
    border-bottom-color: var(--brand);
  }

  .header-cta {
    flex-shrink: 0;
    background: var(--brand);
    color: #fff;
    font-weight: 700;
    font-size: 0.88rem;
    text-decoration: none;
    padding: 0.6rem 1.15rem;
    border-radius: var(--radius-pill);
    transition: background 0.15s ease;
  }

  .header-cta:hover {
    background: var(--brand-strong);
  }

  @media (max-width: 720px) {
    .head-nav {
      display: none;
    }
    .header-cta {
      margin-left: auto;
      padding: 0.55rem 0.9rem;
      font-size: 0.82rem;
    }
  }
</style>
